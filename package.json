{
  "name": "tracksense",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix TrackSense",
    "dev:frontend": "npm run dev --prefix TrackSense",
    "dev:backend": "npm run dev --prefix backend"
  },
  "engines": {
    "node": "24.x"
  }
}
